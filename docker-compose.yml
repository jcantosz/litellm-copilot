services:
  litellm-copilot:
    build: .
    env_file: .env
    ports:
      - "4000:4000"
    volumes:
      # Mount custom config if needed (optional - default config.yaml is in the image)
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
